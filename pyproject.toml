[project]
name = "app"
version = "0.1.0"
dependencies = [ "beautifulsoup4", "fastapi", "httpx", "psycopg[binary]", "pydantic", "python-dotenv", "sqlmodel", "uvicorn" ]
requires-python = ">=3.12,<3.15"
