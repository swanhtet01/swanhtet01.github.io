version: "3.9"
name: supermega-agent-os-retired

# Intentionally no runnable services.
# The former OpenManus/LangGraph prototype exposed default credentials,
# permissive networking, and simulated finance behavior. It is retired.
# Use the canonical managed queue described in agent_os_architecture.md.
services: {}

x-supermega-status:
  state: retired-fail-closed
  replacement: canonical-managed-agent-queue
  external-actions: owner-gated
