apikeys.txt
*.pem
.env*
*credentials*
hyper_unicorn/venv/
.secrets/
pilot-data/*
!pilot-data/
!pilot-data/agent_team_system.json
*.oauth.json
*.token.json
*.service-account.json
showroom/node_modules/
showroom/dist/
showroom/public-app/
showroom/.env*
__pycache__/
*.pyc
.venv-wsl/
.venv/
venv/
swan-intelligence-hub/
.vercel
.vercel_python_packages/
.codex
.tmp/
# No trailing slash: `node_modules/` matches a DIRECTORY only, so an agent worktree's
# `node_modules` SYMLINK slipped past it and was committed on a branch (blob 618a2758,
# an absolute path into one sandbox). Without the slash this matches the symlink too.
node_modules
api-static/
api-static-staging-*/
tmp-*.html
tmp-*.png
.vercel
