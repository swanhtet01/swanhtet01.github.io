# The historical Mark 1 prototype stack is intentionally retired.
#
# It exposed privileged and unauthenticated development services and is not
# part of the canonical four-product SuperMega runtime. Keep the root Compose
# entry point fail-closed so `docker compose up` cannot start obsolete services.
services: {}
